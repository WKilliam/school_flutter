class IdTemperatureMethodes {

  // C° to F° (input * 9 / 5) + 32;
  // F° to C° (input - 32) * (5 / 9);
  // K° to C° (input - 273,15);
  // C° to K° (input + 273,15)
  // K° to F° (input - 273,15) * 9 / 5 + 32;
  // F° to K° (input - 32) * 5 / 9 + 273,15;
}
